# SPA 폴백.
#
# 라우팅이 클라이언트 사이드라서, /auth/oauth 나 /welcome 처럼 index.html 이 아닌
# 경로로 직접 들어오면 Netlify 가 해당 파일을 찾지 못해 404 를 준다.
# 소셜 로그인 콜백이 정확히 이 경우다(백엔드가 브라우저를 /auth/oauth 로 돌려보낸다).
#
# 모든 경로를 index.html 로 넘겨 React Router 가 처리하게 한다.
# 200 이라 URL 은 그대로 유지된다(301/302 와 달리 주소가 바뀌지 않는다).
/*  /index.html  200
